import React from "react";
import styled from "styled-components";

const FeedsListSection = styled.div`
  background: teal;
  height: 200vh;
`;

const FeedsList = () => {
  return <FeedsListSection>FeedsList</FeedsListSection>;
};

export default FeedsList;
